import React from "react";
import Layout from '../../components/layout'

const CarsIndex = ({location, pathContext, history}) => {
  console.log(pathContext)
  return (
    <Layout>
      <div></div>
    </Layout>
  )
}

export default CarsIndex;
